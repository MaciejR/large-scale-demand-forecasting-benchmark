# Local MPS foundation-model inference pipeline (§5.4.3).
# Target: Apple M-series MacBook, Python 3.11, PyTorch MPS backend.
# Install into an isolated venv: python3.11 -m venv ~/venvs/fm-local
#
# Separate from the Azure ML environment because the FM wheels (tirex,
# tabpfn-time-series) pull incompatible torch pins and are not needed
# for the main Azure runs.

# --- torch stack (MPS-compatible on macOS arm64) ---
torch>=2.3,<2.6
numpy>=1.26,<2.0
pandas>=2.1,<2.3
pyarrow>=15

# --- foundation models under test ---
chronos-forecasting[extras]>=2.2  # Chronos-Bolt-Tiny (~9 M) + Chronos-2 (~120 M, covariate-aware)
tabpfn-time-series>=0.2    # TabPFN-TS (~11 M params, covariate-aware)
tirex-ts>=0.3              # TiRex (~35 M params, xLSTM). PyPI name is "tirex-ts"; import stays `from tirex import load_model`. The bare `tirex` PyPI slot is an unrelated project — do NOT use it.
uni2ts                     # Moirai 2.0 Small (~11 M params, decoder-only MoE)
einops>=0.7                # Required by Moirai 2.0 for tensor reshaping
timesfm>=2.0               # TimesFM 2.5 (~200 M params, decoder-only, continuous quantile head)

# --- shared evaluation code paths used by run_gap_filling.py ---
mlflow>=2.14,<3.0
lightgbm>=4.3
scikit-learn>=1.4
tqdm>=4.66

# --- dataset loaders (used by shared data/loaders code) ---
datasets>=2.19
