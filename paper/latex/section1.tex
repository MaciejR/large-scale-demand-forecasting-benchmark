% !TEX root = main.tex
\section{Introduction}
\label{sec:introduction}

The 2024--2026 wave of time-series foundation models ---
\citet{ansari2025chronos2}, TimesFM~2.5 \citep{das2025timesfm2},
Moirai~2.0 \citep{liu2025moirai2}, TiRex \citep{auer2025tirex}
--- has shifted the default assumption in short-term forecasting.
Where a retail team in 2022 would start with LightGBM and a
feature-engineering sprint, the 2026 playbook increasingly says: try
a zero-shot foundation model first, tune LightGBM only if the FM
disappoints. Each model paper reports favourable results on its own
evaluation suite --- GIFT-Eval, fev-bench, or a curated subset of
the M~competitions --- but no paper runs the same battery of models
under matched conditions on the same retail datasets and reports the
delta. The benchmarks multiply; the synthesis does not.

This paper asks: \textbf{when do foundation models actually pay off
for retail demand forecasting?} We operationalise ``pay off'' as a
statistically significant reduction in forecast error relative
to a well-tuned baseline (gradient-boosted tree or statistical model)
under matched evaluation conditions. By ``when'' we mean: on which
datasets, horizons, demand distributions, metrics, and hardware
budgets.

\subsection{Research questions}
\label{sec:research-questions}

We address four research questions across three complementary lenses
(accuracy, moderators, and cost):

\begin{itemize}
  \item \textbf{RQ1 (Accuracy):} Do zero-shot foundation models
    outperform conventional baselines on retail demand forecasting?
    (\S\ref{sec:rq1-accuracy})
  \item \textbf{RQ2 (Moderators):} Which characteristics ---
    metric choice, demand intermittency, model scale, forecast
    horizon --- moderate the FM-vs-baseline gap?
    (\S\ref{sec:rq2-moderators})
  \item \textbf{RQ3 (Cost):} What is the cost-accuracy Pareto
    frontier when consumer-hardware electricity, cloud-GPU list
    price, and CO\textsubscript{2} footprint are included?
    (\S\ref{sec:cost-accuracy})
  \item \textbf{RQ4 (Guidance):} Under what conditions should a
    retail forecasting team deploy a foundation model instead of
    investing in feature engineering for LightGBM?
    (\S\ref{sec:discussion})
\end{itemize}

\subsection{Approach}
\label{sec:approach}

This study combines a PRISMA-informed systematic review with
original gap-filling experiments and structured extraction from two
large forecasting benchmarks (fev-bench, GIFT-Eval).

\begin{enumerate}
  \item \textbf{Source~A (literature + benchmark extraction).} We
    screen \textasciitilde{}150 papers (2020--2026) from Scopus,
    Google Scholar, Semantic Scholar, and arXiv, applying retail-domain
    inclusion criteria. We supplement the extraction with per-model
    per-task results from fev-bench \citep{shchur2025fevbench}
    (20~retail tasks, 7~FMs vs LightGBM/CatBoost baselines) and
    GIFT-Eval \citep{aksu2024gift} (4~Sales-domain tasks, 7~FMs vs
    statistical baselines). The combined extraction yields 780+~rows
    across 9~retail datasets (\S\ref{sec:extraction-schema}).

  \item \textbf{Source~B (gap-filling experiments).} We run our own
    experiments under a unified evaluation protocol
    (\S\ref{sec:experimental-setup}): five foundation models
    (Chronos-Bolt-Tiny, Moirai~2.0-Small, TiRex, Chronos-2,
    TimesFM~2.5; 9--200\,M parameters) on a consumer MacBook
    (Apple~Silicon MPS / CPU), and three baselines
    (lightgbm\_cov, lightgbm\_direct, seasonal\_naive) on Azure~ML.
    Source~B produces gap-filling rows with runtime, cost, and
    CO\textsubscript{2} metadata.
\end{enumerate}

The meta-regression (\S\ref{sec:meta-regression}) pools Source~A and
Source~B into a model-level random-effects model: for each
(FM, baseline, task, metric) pair we compute
$\Delta = \text{FM} - \text{baseline}$, then fit \texttt{rma.mv}
(REML, Knapp--Hartung) with $k = 459$ pairs nested by dataset,
paper, and model name.  Source~A provides both FM and baseline rows
(via fev-bench and GIFT-Eval), contributing the majority of the
paired comparisons.

\subsection{Headline findings}
\label{sec:headline-findings}

Three findings motivate the sections that follow:

\begin{enumerate}
  \item \textbf{The FM advantage is metric-dependent --- strong on
    distributional metrics, not significant on point forecasts.}
    Across $k = 459$ pairs, the per-metric decomposition is the
    primary finding: on Scaled Quantile Loss (SQL),
    $\Dhat = -0.278$ ($p < .0001$); on \WAPE{} (a point-forecast
    metric), $\Dhat = -0.070$ ($p = 0.14$, not significant).  The
    full-pool aggregate ($\Dhat = -0.150$, $p = 0.001$) confirms a
    net FM advantage, but mixing these heterogeneous metrics obscures
    the mechanism: FMs produce well-calibrated predictive
    distributions; their point forecasts trend in the same direction
    but the advantage is attenuated.

  \item \textbf{Model scale is not a significant predictor of retail
    accuracy.}  Within Source~B (controlled protocol), the smallest FM
    (Moirai~2.0-Small, 11\,M) outperforms the largest (Chronos-2,
    120\,M) on M5.  The cross-paper model-scale moderator is
    not significant ($p = 0.663$),
    consistent with \citet{shchur2025fevbench}'s finding that
    architecture matters more than parameter count.

  \item \textbf{The LightGBM protocol gap is conditional on
    intermittency.} \S\ref{sec:baseline-reliability} shows that the
    direct-vs-recursive multi-step gap flips sign at the
    continuous-demand boundary: direct wins on M5 (intermittent) by
    17--22\,\%, recursive wins on Favorita and Rohlik (smooth) by
    3--10\,\%.  This conditional pattern means that no
    retail-benchmark protocol recommendation that ignores demand
    distribution can survive cross-dataset replication.
\end{enumerate}

\subsection{Contributions}
\label{sec:contributions}

\begin{enumerate}
  \item \textbf{PRISMA-informed systematic review} of foundation
    models for retail demand forecasting (780+ extraction rows,
    9~datasets, 2020--2026).
  \item \textbf{Cross-benchmark extraction} from fev-bench and
    GIFT-Eval, yielding 459 model-level paired comparisons across
    14~FMs and conventional baselines.
  \item \textbf{Model-level meta-regression} ($k = 459$) identifying
    a metric-dependent FM advantage: strong on distributional metrics
    (SQL), directionally favorable but not significant on point-forecast
    metrics (\WAPE{}).
  \item \textbf{Baseline reliability analysis} documenting the
    conditional direct-vs-recursive LightGBM gap across three
    datasets.
  \item \textbf{Practitioner decision framework} mapping data
    characteristics to model-family recommendations with
    cost-accuracy Pareto figures on consumer hardware.
\end{enumerate}

\subsection{Paper structure}
\label{sec:paper-structure}

\S\ref{sec:background} surveys the forecasting taxonomy and existing
benchmarks. \S\ref{sec:methods} describes the PRISMA search
protocol, extraction schema, and gap-filling experimental design.
\S\ref{sec:prisma-results} reports the literature extraction results
(PRISMA flow, descriptive statistics). \S\ref{sec:experiments}
presents the gap-filling experiments:
\S\ref{sec:experimental-setup}--\ref{sec:per-dataset-results}
experimental setup and per-dataset results,
\S\ref{sec:baseline-reliability} baseline reliability (direct vs
recursive), \S\ref{sec:fm-consumer-box} foundation model
consumer-box results. \S\ref{sec:meta-regression} runs the
meta-regression: cross-paper pooled deltas
(\S\ref{sec:cross-paper-pooling}), moderator hypotheses
(\S\ref{sec:moderator-results}),
cost-accuracy Pareto frontier (\S\ref{sec:cost-accuracy}),
heterogeneity diagnostics (\S\ref{sec:heterogeneity}), and
preliminary findings (\S\ref{sec:rq1-accuracy}).
\S\ref{sec:discussion} translates the findings into a practitioner
decision framework. \S\ref{sec:limitations} discusses limitations
(single author, metric sensitivity, consumer-HW-only Source~B).
\S\ref{sec:conclusion} concludes with per-RQ answers and future
work.
